# CLAUDE.md

This project's working agreement for AI agents lives in
[`AGENTS.md`](AGENTS.md). Read it before starting any work.

It is written to apply to any AI agent, not just Claude, so it is kept in one
shared file rather than duplicated here. Do not add Claude-specific rules to
this file; update `AGENTS.md` instead so every agent sees the same guidance.
